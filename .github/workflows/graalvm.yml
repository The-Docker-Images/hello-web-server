# Remove this CI action if not required
name: graalvm-test

on:
  push:
    branches: '*'
  pull_request:
    branches: '*'
  # daily check at 01:00 AM
  schedule:
    - cron: '0 1 * * *'

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: DeLaGuardo/setup-graalvm@3
      with:
        graalvm-version: '20.2.0.java8'

    - name: Cache node modules
      uses: actions/cache@v1
      env:
        cache-name: cache-node-modules
      with:
        path: ~/.npm
        key: ${{ runner.os }}-build-quick-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}

    - run: npm ci
    - run: npm run lint
    - run: npm run build
    - run: npm run test

